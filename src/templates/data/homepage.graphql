query {
  entries: getEntryList(sort: {field: "_createdAt", order: "desc"}) {
    items {
      message,
      _createdAt
    }
  }
  home: getHome {
    heroImage {
      path
    }
    welcomeMessage
  }
}
